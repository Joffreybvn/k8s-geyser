apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "geyser.fullname" . }}-test-connection"
  labels:
    {{- include "geyser.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "geyser.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
